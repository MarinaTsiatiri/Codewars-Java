import java.util.Arrays;

class Kata{
  public static long sumTwoSmallestNumbers(long[] numbers) {
    int lowSum;
    Arrays.sort(numbers);
    lowSum = (int)(numbers[0]) + (int)(numbers[1]);
    return lowSum; 
  }             
}
